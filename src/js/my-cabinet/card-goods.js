import GoodsTmpl from '../../templates/card-my-goods.hbs';

const goodsContainer = document.querySelector('.my-goods-hbs');

// console.log(goodsContainer);

const user = {
  foto: 'foto',
  name: 'Рослина монстера',
  price: '30',
};

// console.log();

// goodsContainer.innerHTML = GoodsTmpl(user);
